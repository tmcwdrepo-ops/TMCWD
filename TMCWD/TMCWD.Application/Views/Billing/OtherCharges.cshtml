@{
    Layout = "_InsideLayout";
    ViewData["PageTitle"] = "Other Charges";
    ViewData["ActivePage"] = "other-charges";
}

<div class="panel" style="padding: var(--space-lg); border-radius: var(--radius-md);">
    <h2 style="font-size: var(--font-size-lg); font-weight: 600; margin-bottom: var(--space-sm);">Other Charges</h2>
    <p style="color: var(--color-text-dim); font-size: var(--font-size-base);">
        Manage other billing charges and fees. Select a sub-option from the sidebar menu to proceed.
    </p>
</div>
