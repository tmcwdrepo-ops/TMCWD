@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = "_insideLayout";
}

<div id="accordion-collapse-readingsheet" data-accordion="collapse" class="rounded-lg border border-default overflow-hidden shadow-xs">
    <h2 id="accordion-collapse-heading-1" class="bg-gray-300">
        <button id="accordion-button-item-1" type="button" class="collapsible-button flex items-center justify-between w-full p-5 font-medium rtl:text-right text-body rounded-t-base border border-t-0 border-x-0 border-b-default hover:text-heading hover:bg-neutral-secondary-medium gap-3" data-accordion-target="#accordion-collapse-body-1" aria-expanded="true" aria-controls="accordion-collapse-body-1">
            <span>Reading Sheet</span>
            <svg id="accordion-icon-item-1" data-accordion-icon class="w-5 h-5 shrink-0" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24"><path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m5 15 7-7 7 7" /></svg>
        </button>
    </h2>
    <div id="accordion-collapse-body-1" class="collapsible-body bg-white border border-s-0 border-e-0 border-t-0 border-b-default" aria-labelledby="accordion-collapse-heading-1">
        <div class="grid grid-cols-2">
            <div>
                Zone
            </div>
            <div>
                <!--Zone Dropdown-->
                <div>
                    <div class="relative w-full">
                        <input type="hidden" class="selected-value" />
                        <select id="cmbZone" class="hidden"></select>
                        <div class="relative grid grid-cols-[95%_5%] border rounded-md gap-2 text-sm px-4 py-2">
                            <div class="w-full text-left">
                                <input type="text" class="dropdown-input w-full focus:outline-none" placeholder="Select Material" />
                            </div>
                            <div class="w-full text-right">
                                <button type="button" class="border-none">
                                    <i class="fa-solid fa-chevron-down"></i>
                                </button>
                            </div>
                        </div>
                        <div id="dropdown-options" class="absolute dropdown-options w-full mt-1 border rounded-md py-2 px-2 bg-white max-h-64 overflow-y-scroll hidden">
                            <p class="w-full hover:bg-blue-500">No materials</p>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                Book
            </div>
            <div>
                <!--Book dropdown-->
                <div>
                    <div class="relative w-full">
                        <input type="hidden" class="selected-value" />
                        <select id="cmbBook" class="hidden"></select>
                        <div class="relative grid grid-cols-[95%_5%] border rounded-md gap-2 text-sm px-4 py-2">
                            <div class="w-full text-left">
                                <input type="text" class="dropdown-input w-full focus:outline-none" placeholder="Select Material" />
                            </div>
                            <div class="w-full text-right">
                                <button type="button" class="border-none">
                                    <i class="fa-solid fa-chevron-down"></i>
                                </button>
                            </div>
                        </div>
                        <div id="dropdown-options" class="absolute dropdown-options w-full mt-1 border rounded-md py-2 px-2 bg-white max-h-64 overflow-y-scroll hidden">
                            <p class="w-full hover:bg-blue-500">No materials</p>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                Billing Period
            </div>
            <div>
                
            </div>
            <div>
                Assigned To
            </div>
            <div>
                <!--Dropdown Meter Reader drop down-->
                <div>
                    <div class="relative w-full">
                        <input type="hidden" class="selected-value" />
                        <select id="cmbAssignedTo" class="hidden"></select>
                        <div class="relative grid grid-cols-[95%_5%] border rounded-md gap-2 text-sm px-4 py-2">
                            <div class="w-full text-left">
                                <input type="text" class="dropdown-input w-full focus:outline-none" placeholder="Select Material" />
                            </div>
                            <div class="w-full text-right">
                                <button type="button" class="border-none">
                                    <i class="fa-solid fa-chevron-down"></i>
                                </button>
                            </div>
                        </div>
                        <div id="dropdown-options" class="absolute dropdown-options w-full mt-1 border rounded-md py-2 px-2 bg-white max-h-64 overflow-y-scroll hidden">
                            <p class="w-full hover:bg-blue-500">No materials</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="grid col-span-2">
                <button id="btnSave" type="button" class="p-2 border rounded-md"></button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    document.addEventListener("DOMContentLoaded", function(event) {

        var selectedZoneItem = null;
        var selectedBookItem = null;
        var selectedUserItem = null

        const accordion = document.getElementById('accordion-collapse-readingsheet');
        accordion.Collapsible();


        const cmbZone = document.getElementById("cmbZone");

        if(cmbZone){

            cmbZone.SearchableDropdown({
                getItemUrl: '@Url.Action("GetZones", "ZoneBook")',
                getItemParameter : null,
                dataMemberText: 'zone',
                dataMemberValue: 'zone',
                onSelectChange : (selectedItem) => {
                    selectedZoneItem = selectedItem;
                },
                onSelectionClear: () => {
                    selectedZoneItem = null;
                }
            });

            // var zones = zoneClient.getAsync();
            // if(zones){
            //     const dropdownOptions = document.getElementById('dropdown-options');
            //     dropdownOptions.replaceChildren();
            //     [...zones].forEach((zone) => {
            //         const lnk = document.createElement('a');
            //         lnk.href = '#';
            //         const par = document.createElement('p');
            //         par.classList.add('w-full', 'px-2', 'py-2', 'hover:bg-blue-500', 'hover:text-white');
            //         par.textContent = zone.zone;
            //         lnk.appendChild(par);
            //         lnk.dataset.itemid = zone.id;
            //         lnk.dataset.itemname = zone.name;
            //         //lnk.dataset.unitsellingprice = zone.unitSellingPrice;
            //         dropdownOptions.appendChild(lnk);
            //     });
            // }
        }

        const cmbBook = document.getElementById('cmbBook');

        if(cmbBook){
            cmbBook.SearchableDropdown({
                getItemUrl: '@Url.Action("GetZones", "ZoneBook")',
                getItemParameter: null,
                dataMemberText: 'book',
                dataMemberValue: 'book',
                onSelectChange: (selectedItem) => {
                    selectedBookItem = selectedItem;
                },
                onSelectionClear: () => {
                    selectedBookItem = null;
                }
            });
        }

        const cmbAssignedTo = document.getElementById('cmbAssignedTo');

        if (cmbAssignedTo){
            cmbAssignedTo.SearchableDropdown({
                getItemUrl: '@Url.Action("GetUsersByRole", "Admin")' + "?role=4",
                getItemParameter: null,
                dataMemberText: 'name',
                dataMemberValue: 'id',
                onSelectChange: (selectedItem) => {
                    selectedUserItem = selectedItem
                },
                onSelectionClear: () => {
                    selectedUserItem = null;
                }
            });
        }

        const btnSave = document.getElementById("btnSave");

        if (btnSave) {
            btnSave.addEventListener("click", (evt) => {
                evt.preventDefault();
                evt.stopPropagation();

                var url = '@Url.Action("CreateReadingSheet", "ReadingSheet")' + "?zone=" + zone + '&book=' + book;

                var createSheetClient = new WebClient('@Url.Action("CreateReadingSheet", "ReadingSheet")');

            });
        }

    });
</script>